### Add daemon-owned app state defaults [23:23:47] <u1ipk7yeq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Implement the minimal shape of app_state and default the selected model to minimax/minimax-m2.5:free.

**Implementation summary:**
- Added a new xoxo-core app_state module with AppState and a file-backed AppStateRepository.
- Default app state now points new root-chat model selection at minimax/minimax-m2.5:free.

**Explicit user constraints:**
- Keep app state separate from static config and owned by the daemon side.

---

### Store current provider in app state [23:24:40] <e51ajmnfq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Extend app state so it also carries information about the current provider with minimum effort.

**Implementation summary:**
- Added a separate current_provider field to AppState alongside current_model.
- Kept the default provider and default model aligned on minimax/minimax-m2.5:free.

**Explicit user constraints:**
- Use the minimum-effort shape without expanding scope beyond app-state structs.

---

### Hard-code OpenRouter app-state default [23:25:18] <udbc5y4fq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Hard-code, for now, that the current provider will be OpenRouter.

**Implementation summary:**
- Changed the app-state default provider from minimax to OpenRouter.
- Kept the default selected model string unchanged while pointing it at the OpenRouter provider object.

**Explicit user constraints:**
- Use a hard-coded default for now instead of introducing provider selection logic.

---

### Persist current provider and model in config [23:27:51] <6k0ued3hq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Update the xoxo-core config module to carry the current provider and current model, and reflect that better in ~/.xoxo/config.toml.

**Implementation summary:**
- Added current_provider and current_model sections to the Config shape with minimal accessors and coverage.
- Prepared the config file shape so the daemon-owned provider and model selection are persisted directly in config.toml.

**Explicit user constraints:**
- Keep the config-side representation minimal and aligned with the current OpenRouter plus minimax defaults.

---

### Initialize app state from config values [23:31:17] <ujjxrxpkq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Revert the hard-coded app-state model and provider defaults and make them read from the current config instead.

**Implementation summary:**
- Replaced AppState hard-coded defaults with AppState::from_config built from the persisted config snapshot.
- Updated AppStateRepository::load_or_create to seed app-state.json from load_config() instead of static defaults.

**Explicit user constraints:**
- Keep the change minimal and derive provider compatibility from the current config shape.

---

### Export nerd base prompt factory [23:33:28] <ietmvwdmq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Add to the nerd crate an exportable prompt factory function that accepts model name and tool schemas and returns a merged base prompt.

**Implementation summary:**
- Added a new nerd::prompt module with build_base_prompt(model_name, tool_schemas).
- Exported the prompt factory from nerd crate root and rendered a minimal dummy prompt with tool names and descriptions.

**Explicit user constraints:**
- Use a simple placeholder prompt text and keep the implementation minimal.

---

### Bootstrap root chats from first user message [23:35:29] <up7w6sxnq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** When the first user message comes in, start a root agent by using the nerd base prompt as the system prompt and the incoming text as the first user prompt.

**Implementation summary:**
- Updated the daemon submit-message path so active_chat_id = None creates a new root chat path and publishes a system prompt before the first user message.
- Loaded current model from app state and built the system prompt with nerd::build_base_prompt plus registered tool schemas.

**Explicit user constraints:**
- Keep the runtime stubbed and use a simple assistant ok response after bootstrapping the root conversation.

---

### Execute real LLM completions for root chats [23:45:03] <6cg4dd9uq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Replace the stub root-chat assistant response with the real model answer by implementing the missing completion seam in xoxo-core::llm.

**Implementation summary:**
- Added LlmFacade::complete with runtime execution paths for rig and ai-lib backends, including request and response mapping.
- Updated the daemon root-chat bootstrap flow to call the facade and publish the real assistant message instead of a stub ok reply.

**Explicit user constraints:**
- Keep the implementation minimal and focus on the current root-chat path driven by persisted current provider and model selection.

---

### Surface daemon errors in the TUI [23:49:55] <628ngjzyq7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Fix the case where the TUI shows absolutely no response, not even an error message.

**Implementation summary:**
- Changed the daemon loop to catch per-command errors, publish them as bus error events, and keep the daemon alive.
- Used the existing BusPayload::Error path so the TUI can render setup and completion failures instead of failing silently.

**Explicit user constraints:**
- Keep the fix minimal and focused on making failures visible in the current TUI-driven daemon flow.

---

### Wrap TUI message history lines [23:53:37] <ay6zwct1q7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Make the content of the message history block in xoxo-tui word-wrap.

**Implementation summary:**
- Enabled ratatui paragraph wrapping for the conversation history pane.

**Explicit user constraints:**
- Keep the change minimal and local to the TUI history block renderer.

---

### Show current provider and model in TUI header [23:56:23] <a04jkyx3q7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** Make the hardcoded current model and provider names in the TUI header reflect the actual app state.

**Implementation summary:**
- Loaded current provider and model from config into the TUI app state.
- Replaced hardcoded header labels with template placeholders rendered from the live app state.

**Explicit user constraints:**
- Keep the fix minimal and local to the TUI header rendering path.

---

### Hide system messages in the TUI [23:58:08] <qw172494q7>

🟢 OpenAI Codex (`gpt-5.4`)

**User request summary:** System messages should not be displayed in the TUI.

**Implementation summary:**
- Filtered system-role bus messages out of the TUI history rendering path.

**Explicit user constraints:**
- Keep system messages on the runtime side and only hide them from the TUI display.