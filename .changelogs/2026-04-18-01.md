### Show focused cursor in xoxo input [10:17:53] <e3ir7fp2e6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-tui

**User request summary:** Make  show a cursor in the input textarea so it looks focused.

**Implementation summary:**
- Used  to place the terminal cursor at the end of the current input text inside the textarea.
- Kept the change scoped to the main layout renderer in  and verified it with .

---

### Add hedgehog header to xoxo layout [10:30:08] <qk0kk12bf6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-tui

**User request summary:** Add headers to the xoxo-tui layout using the content from the hedgehog header file for the left-side header treatment.

**Implementation summary:**
- Added a dedicated 4-line header region to the main xoxo-tui layout and rendered the embedded hedgehog ASCII header above the conversation history.
- Shifted the history and input sections down to preserve the existing input area behavior, including the visible cursor placement in the textarea.
- Verified the updated layout compiles with cargo check -p xoxo-tui.

---

### Align xoxo header with banner changes [10:37:18] <ex7qmdkhf6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-tui

**User request summary:** Align the xoxo-tui UI layout with the updated hedgehog banner.

**Implementation summary:**
- Replaced the hardcoded header height with a runtime line count derived from the embedded hedgehog banner so the main layout follows banner size changes automatically.
- Kept the existing history, input, and visible cursor behavior intact while updating only the header sizing logic.
- Verified the crate still compiles with cargo check -p xoxo-tui.

---

### Add colored header and input prompt [10:39:18] <6mxosr3if6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-tui

**User request summary:** Implement different header colors and prepend the bottom input with a prompt character like >.

**Implementation summary:**
- Updated the xoxo-tui header renderer to split each banner line around the separator and color the ASCII art and right-side status text differently.
- Prepended the input textarea display with a > prompt and adjusted cursor placement so the cursor still tracks the typed input correctly.
- Verified the crate compiles with cargo check -p xoxo-tui.

---

### Resolve banner version and cwd placeholders [10:42:38] <ehflbgnlf6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-tui

**User request summary:** Replace the {VERSION} and {PWD} placeholders in the xoxo-tui hedgehog banner with the actual Cargo package version and current working directory.

**Implementation summary:**
- Updated the banner rendering path to materialize the embedded hedgehog template before drawing, replacing {VERSION} with env!(CARGO_PKG_VERSION) and {PWD} with the runtime current working directory.
- Kept the existing colored header rendering and dynamic header sizing logic intact after placeholder substitution.
- Verified the crate compiles with cargo check -p xoxo-tui.

---

### Parse ANSI banner styling in xoxo header [10:51:06] <a0ini04rf6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-tui

**User request summary:** Patch xoxo-tui so the header banner respects ANSI color markers from the hedgehog file.

**Implementation summary:**
- Added the ansi-to-tui dependency and switched header rendering to parse ANSI-styled banner text into ratatui text spans before rendering.
- Translated literal hedgehog template markers from \033[... to real escape sequences after substituting the VERSION and PWD placeholders.
- Verified the updated crate builds with cargo check -p xoxo-tui after fetching the new dependency.

---

### Add serde-safe work item bus messages [12:53:47] <e34zlf2di6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo-core #bus

**User request summary:** Implement only the work-item user-assistant communication bus between core and tui.

**Implementation summary:**
- Replaced empty bus payloads with serde-safe `UserMessage`, `AgentMessage`, and `WorkId` types for work-item conversation flow.
- Renamed bus send/publish/subscribe methods to reflect user and agent message traffic and added focused bus tests.

**Explicit user constraints:**
- Keep the scope to work items and user-assistant communications only.
- Messages must stay serde-serializable for future external transports.

---

### Show core hello message in TUI history [12:55:51] <yksurdnfi6>

🟢 OpenAI Codex (`gpt-5.4`)

**Tags:** #xoxo #xoxo-tui #bus

**User request summary:** Make the core send a hello message to the bus and have the TUI display it in the message history panel.

**Implementation summary:**
- Published an initial `AgentMessage::WorkCompleted` from the in-process core bus during TUI startup.
- Added TUI-side handling for `AgentMessage` values and drained the agent-message receiver into the history panel each frame.

**Explicit user constraints:**
- Keep the implementation minimal and use the existing work-item conversation bus.