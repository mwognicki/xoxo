### Add concurrent read-only tool batching [03:28:37] <acdzss5q4b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/concurrent-tools]

**Tags:** #tools #agentix

**User request summary:** Implement a new agentix meta-tool that runs multiple independent invocations of the same non-mutating tool concurrently, with upfront validation and compound partial-failure results.

**Implementation summary:**
- Added explicit tool metadata and exposed the caller's allowed tool set through ToolContext so orchestrating tools can validate read-only concurrent eligibility.
- Implemented the new agentix tool `run_tools_concurrently` with upfront rejection for unavailable or ineligible tools and compound per-invocation success or error results.
- Marked `read_file` and `inspect_code_structure` as read-only and safe for concurrent invocation, added tests for the new tool, and verified xoxo-core, agentix, and nerd test targets.

**Explicit user constraints:**
- Concurrent batches may only target one tool kind at a time.
- Only read-only tools explicitly marked safe for concurrent invocation are allowed.
- Invalid batches must fail the whole meta-call up front, while runtime child-call failures stay inside the compound result.

---

### Add PHP support to noise stripper [03:35:18] <2tzgzt9v4b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-noise-stripper]

**Tags:** #noise-stripper #php

**User request summary:** Add PHP as a new language in `crates/agents/nerd/src/coding/noise_stripper/mod.rs`.

**Implementation summary:**
- Added PHP extension detection and dispatcher wiring in the noise stripper module.
- Added a dedicated PHP comment stripper covering `//`, `#`, and `/* */` comments while preserving string contents and text shape.
- Added PHP-focused unit coverage and verified the `noise_stripper` test target for the `nerd` crate.

---

### Extend noise stripper with C-family support [03:37:00] <6cdfspkx4b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-noise-stripper]

**Tags:** #noise-stripper #c-family

**User request summary:** Extend the noise stripper with the C language family noise stripping.

**Implementation summary:**
- Added a shared C-family noise stripper that removes `//` and `/* */` comments while preserving strings, char literals, C++ raw strings, and source text shape.
- Extended extension-based language detection and dispatch for common C and C++ source and header file extensions.
- Added C-family unit coverage and verified the `noise_stripper` test target for the `nerd` crate.

---

### Extend noise stripper with more languages [03:40:47] <eless0g04b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-noise-stripper]

**Tags:** #noise-stripper #languages

**User request summary:** Extend the noise stripper with C#, Ruby, Bash, GraphQL, Java, and Solidity.

**Implementation summary:**
- Added dedicated noise strippers for C#, Ruby, Bash-like shell scripts, and GraphQL with syntax-aware handling for each language family.
- Extended extension-based detection and dispatcher routing for C#, Ruby, shell, GraphQL, Java, and Solidity files, reusing the shared C-family stripper for Java and Solidity.
- Added unit coverage for detection and representative stripping behavior across the new languages, and verified the `noise_stripper` test target for the `nerd` crate.

---

### Extend noise stripper with more JVM and BEAM languages [03:44:17] <q04nxl524b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-noise-stripper]

**Tags:** #noise-stripper #languages

**User request summary:** Add noise stripping for Kotlin, Swift, Elixir, and Groovy.

**Implementation summary:**
- Added dedicated noise strippers for Kotlin, Swift, Elixir, and Groovy with syntax-aware handling for their multiline and raw string forms.
- Extended extension-based detection and dispatcher routing for `.kt`, `.kts`, `.swift`, `.ex`, `.exs`, `.groovy`, and `.gradle` files.
- Added unit coverage for representative stripping behavior across the new languages and verified the `noise_stripper` test target for the `nerd` crate.

---

### Extend noise stripper with more systems languages [03:53:01] <2fbcucu94b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-noise-stripper]

**Tags:** #noise-stripper #languages

**User request summary:** Add noise stripping for Dart, Haskell, Erlang, Fortran, Julia, Lua, Scala, and VB.net.

**Implementation summary:**
- Added dedicated noise strippers for Dart, Haskell, Erlang, Fortran, Julia, Lua, Scala, and VB.NET with syntax-aware handling for their comment forms and key string forms.
- Extended extension-based detection and dispatcher routing for `.dart`, `.hs`, `.erl`, `.hrl`, Fortran source extensions, `.jl`, `.lua`, `.scala`, `.sc`, and `.vb` files.
- Added unit coverage for representative stripping behavior across the new languages and verified the `noise_stripper` test target for the `nerd` crate.

---

### Remove app-state model persistence path [04:00:13] <y8qhmrcf5b>

🟢 OpenAI Codex (`gpt-5.4`) [main]

**Tags:** #xoxo-core #xoxo-tui

**User request summary:** Stop using the app state JSON file as the source of truth for model selection; use config for new conversations and sled-stored conversation models for existing chats.

**Implementation summary:**
- Removed the xoxo-core app_state module and stopped persisting runtime provider/model defaults in app-state.json.
- Updated daemon startup to derive the provider config and new-chat model directly from load_config().
- Updated the TUI to fall back to config defaults when no persisted conversation is restored, preserving sled-backed per-chat model display.

**Explicit user constraints:**
- The single source of truth for new conversation model selection must be the config file, while per-conversation model state must come from sled persistence.

---

### Add MCP server config with env-backed values [04:34:44] <a4cqmds55b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #config #mcp

**User request summary:** Add MCP server configuration to xoxo-core with stdio, http, and sse transports, remember future OAuth-guarded servers, and use env-extensions in the config model.

**Implementation summary:**
- Added reusable env-backed string config values plus MCP server/auth/transport structs under xoxo-core config.
- Extended the top-level Config with MCP server accessors and covered the new config shapes with unit tests and doctests.

**Explicit user constraints:**
- Support all three MCP transports: stdio, http, and sse.
- Keep OAuth authentication future-facing without implementing the auth flow itself.
- Model MCP configuration using env-extensions rather than raw-only string fields.

---

### Add ADR for xoxo MCP client architecture [04:41:11] <aulckspa6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #docs #adr #mcp

**User request summary:** Write the MCP integration plan as a new docs/adr ADR document.

**Implementation summary:**
- Added ADR 0004 describing how xoxo will use rust-mcp-sdk behind an internal MCP abstraction layer.
- Documented transport mapping, auth boundaries, phased rollout, and why MCP tools should adapt into the existing xoxo tool model.

---

### Add initial xoxo MCP client runtime boundary [04:48:56] <2zd8njng6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #mcp #config #runtime

**User request summary:** Implement the first MCP client slice in xoxo-core by adding the runtime boundary proposed in the ADR.

**Implementation summary:**
- Added a new xoxo-core mcp module with a xoxo-owned client session wrapper, error types, and lightweight server/tool descriptors.
- Wired MCP config into rust-mcp-sdk client construction for stdio, streamable http, and sse, with connect/list-tools/shutdown support.
- Added targeted tests for transport construction and explicit runtime errors for unimplemented auth and stdio cwd support.

**Explicit user constraints:**
- Use rust-mcp-sdk behind a xoxo-owned abstraction rather than leaking SDK types through the rest of the runtime.
- Keep OAuth-guarded MCP servers in scope architecturally while not implementing the auth flow in this step.

---

### Revise MCP ADR toward lazy discovery [04:55:32] <au7u2epl6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #docs #adr #mcp

**User request summary:** Update the MCP ADR to reflect a lazy-loading MCP integration plan inspired by lazy-mcp.

**Implementation summary:**
- Revised ADR 0004 to prefer lazy MCP discovery and generic invocation over eager registration of every remote MCP tool.
- Added selective-promotion guidance so only high-value MCP tools would become first-class xoxo tools later.

**Explicit user constraints:**
- Keep rust-mcp-sdk as the transport/protocol engine behind xoxo-owned boundaries.
- Retain future OAuth support as an xoxo-owned concern while client OAuth remains unavailable in the SDK.

---

### Add lazy MCP discovery catalog and tools [05:00:03] <q6m4365o6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #mcp #tooling #runtime

**User request summary:** Implement the Phase 2 MCP slice by adding lazy discovery for configured MCP servers and their tools.

**Implementation summary:**
- Added a shared lazy MCP catalog that lists configured servers, caches per-server discovery results, and resolves sessions on demand.
- Added xoxo discovery tools for listing MCP servers, listing a server's remote tools, and describing a specific MCP tool.
- Kept MCP integration lazy by default: no eager registration of every remote MCP tool, only discovery meta-tools in the normal registry.

**Explicit user constraints:**
- Follow the revised ADR direction toward lazy MCP discovery rather than eager remote tool registration.

---

### Add generic invoke_mcp_tool execution bridge [05:02:45] <uvij8e8q6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #mcp #tooling #runtime

**User request summary:** Implement the single generic invoke_mcp_tool bridge for lazy MCP integration.

**Implementation summary:**
- Added MCP session and catalog support for invoking a specific remote tool with JSON object arguments.
- Added the invoke_mcp_tool meta-tool so agents can execute lazily discovered MCP tools without eager registration.
- Returned raw serialized MCP tool results to preserve server-specific response structure while keeping the bridge generic.

**Explicit user constraints:**
- Keep MCP integration lazy by default and avoid eagerly converting every remote MCP tool into a first-class xoxo tool.

---

### Add lazy MCP usage guidance to nerd prompt [05:05:25] <ut8hc09s6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #prompt #mcp #agents

**User request summary:** Improve the prompt guidance so agents understand the MCP discovery and invoke sequence well.

**Implementation summary:**
- Added conditional MCP guidance to the nerd base prompt when lazy MCP meta-tools are available.
- Documented the intended discovery flow: list servers, inspect tools, describe schemas when needed, then invoke with JSON object arguments.

**Explicit user constraints:**
- Keep the guidance lean and only include it when MCP tools are actually present in the prompt tool list.

---

### Gate MCP prompt guidance on available servers [05:06:44] <6iygw99t6b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mcp-setup]

**Tags:** #prompt #mcp #agents

**User request summary:** Ensure the MCP-related prompt partials are used if and only if there is at least one MCP server available.

**Implementation summary:**
- Changed the nerd prompt API to accept an explicit has-available-mcp-servers signal instead of inferring MCP availability only from tool schemas.
- Updated daemon prompt construction to enable MCP guidance only when the loaded config contains at least one configured MCP server.