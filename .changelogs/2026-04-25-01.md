### Add concurrent read-only tool batching [03:28:37] <acdzss5q4b>

🟢 OpenAI Codex (`gpt-5.4`) [feat/concurrent-tools]

**Tags:** #tools #agentix

**User request summary:** Implement a new agentix meta-tool that runs multiple independent invocations of the same non-mutating tool concurrently, with upfront validation and compound partial-failure results.

**Implementation summary:**
- Added explicit tool metadata and exposed the caller's allowed tool set through ToolContext so orchestrating tools can validate read-only concurrent eligibility.
- Implemented the new agentix tool `run_tools_concurrently` with upfront rejection for unavailable or ineligible tools and compound per-invocation success or error results.
- Marked `read_file` and `inspect_code_structure` as read-only and safe for concurrent invocation, added tests for the new tool, and verified xoxo-core, agentix, and nerd test targets.

**Explicit user constraints:**
- Concurrent batches may only target one tool kind at a time.
- Only read-only tools explicitly marked safe for concurrent invocation are allowed.
- Invalid batches must fail the whole meta-call up front, while runtime child-call failures stay inside the compound result.
