### Split xoxo TUI UI rendering modules [13:57:33] <epuzv6im38>

🟢 OpenAI Codex (`gpt-5.4`) [main]

**Tags:** #xoxo-tui

**User request summary:** Split overloaded xoxo-tui ui.rs logic into logically viable smaller parts.

**Implementation summary:**
- Kept ui.rs focused on the public draw entry point, header rendering, viewport composition, input rendering, modal rendering, and alternate layout rendering.
- Moved conversation history rendering and related tests into ui/history.rs.
- Moved markdown-to-ratatui rendering into ui/markdown.rs and generic tool line helpers into ui/tool_lines.rs while preserving the existing tool_format interface.

**Explicit user constraints:**
- Use the snip proxy for shell commands.

---

### Split xoxo TUI app state modules [14:00:55] <u9aevt3o38>

🟢 OpenAI Codex (`gpt-5.4`) [main]

**Tags:** #xoxo-tui

**User request summary:** Apply similar splitting to the overloaded xoxo-tui app.rs file.

**Implementation summary:**
- Kept app.rs focused on the App and LayoutMode state surface, constructor, and simple accessors.
- Moved keyboard and mouse event handling plus command reset tests into app/events.rs.
- Moved bus synchronization and session reset logic into app/sync.rs, restored chat history shaping into app/history.rs, and derived model stats into app/stats.rs.

**Explicit user constraints:**
- Use the snip proxy for shell commands.

---

### Grow wrapped TUI input field [15:30:23] <qmkectll58>

🟢 OpenAI Codex (`gpt-5`) [feat/fixes-to-tui-layout]

**Tags:** #xoxo-tui

**User request summary:** Make the xoxo TUI input field word-wrap and grow while reducing the conversation pane with scrolling.

**Implementation summary:**
- Computed input pane height from wrapped input content and let the conversation pane shrink through the vertical layout.
- Rendered input as pre-wrapped visual lines with bottom scrolling and cursor placement on the visible wrapped line.
- Added focused wrapping tests and a direct unicode-width dependency for terminal display width calculations.

---

### Highlight TUI code snippets [17:28:25] <y2b112y378>

🟢 OpenAI Codex (`gpt-5`) [feat/fixes-to-tui-layout]

**Tags:** #xoxo-tui #syntax-highlighting

**User request summary:** Format code snippets in the conversation history using the syntax highlighter from xoxo-core.

**Implementation summary:**
- Imported `xoxo_core::syntax_highlighter::highlight_syntax` in `ui/history.rs` and passed it into assistant markdown rendering.
- Updated markdown code block rendering to convert the core ANSI-highlighted output into Ratatui spans.
- Added a history-level test proving fenced Rust code receives styled spans through the conversation rendering path.

**Explicit user constraints:**
- Use the shared core syntax highlighter instead of local TUI-only formatting.