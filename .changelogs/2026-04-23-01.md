### Wire final AST grammar integrations [06:53:42] <erc8qd3uja>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #tree-sitter #ast

**User request summary:** Wire Solidity, GraphQL, ASM, Proto, and Pug tree-sitter grammars into the nerd AST tooling where compatible.

**Implementation summary:**
- Added AST language detection, parser dispatch, symbol inspectors, and find_symbol schema entries for Solidity, GraphQL, ASM, and Proto.
- Added tests covering language detection, per-grammar structure extraction, and cross-language find_symbol results for the new compatible grammars.

**Explicit user constraints:**
- Left Pug unwired because tree-sitter-pug 0.0.1 depends on tree-sitter 0.17 and exposes an incompatible Language type for the current parser stack.

---

### Extract nerd AST crate boundary [07:02:28] <qgnuuzs1ja>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #rust

**User request summary:** Move the AST-related slice out of nerd into a separate nerd-ast crate while keeping it strictly part of Nerd.

**Implementation summary:**
- Added crates/agents/nerd-ast as a workspace crate owning AST language detection, parser dispatch, structure types, and find_symbol implementation.
- Moved tree-sitter grammar dependencies from nerd into nerd-ast and updated Cargo.lock/workspace dependency wiring.
- Kept nerd tool adapters importing through crate::coding by re-exporting nerd_ast types and functions from the existing coding facade.

**Explicit user constraints:**
- Named the crate nerd-ast to reflect that AST support remains Nerd-owned rather than a general shared platform crate.

---

### Add AST-backed symbol rename tool [07:10:40] <628e7227ja>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tooling

**User request summary:** Implement a deterministic rename_symbol(file_path, symbol, replacement) tool backed by AST deterministic intelligence.

**Implementation summary:**
- Added nerd-ast rename_symbol_in_content support that verifies an AST-visible definition before rewriting identifier-like tree-sitter leaves in one source file.
- Registered a nerd rename_symbol tool that performs file IO, execution-context md5 checks, writes the renamed content, and returns bounded rename metadata.
- Added focused tests for the pure AST rename engine and the file-mutating tool adapter.

---

### Separate nerd AST language modules [07:14:12] <envjvdsaka>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tree-sitter

**User request summary:** Extract the tree-sitter language implementations from nerd-ast into a submodule so they are separated from utility code in that crate.

**Implementation summary:**
- Moved grammar-specific AST inspectors under crates/agents/nerd-ast/src/languages and added a languages module for parser dispatch.
- Centralized parser language setup in the languages submodule so rename_symbol and inspect_code_structure no longer carry direct grammar-binding dispatch at the crate root.
- Kept find_symbol, rename_symbol, language detection, and shared structures as root-level nerd-ast utilities.

---

### Move nerd AST tools into tools module [07:16:13] <q28dp4bcka>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tools

**User request summary:** Move crates/agents/nerd-ast/src/rename_symbol.rs and crates/agents/nerd-ast/src/find_symbol.rs into nerd-ast/src/tools.

**Implementation summary:**
- Added a nerd-ast tools module and moved find_symbol and rename_symbol implementations under crates/agents/nerd-ast/src/tools.
- Kept root-level nerd_ast re-exports stable so downstream nerd tool adapters continue to compile without API changes.

---

### Add AST-backed symbol patch tool [07:20:35] <6q02l7nfka>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tooling

**User request summary:** Add a patch_symbol(file_path, symbol, replacement) AST-specific deterministic tool.

**Implementation summary:**
- Added nerd-ast patch_symbol_in_content to replace exactly one AST-visible symbol definition range and reject missing, ambiguous, invalid-source, or invalid-replacement cases.
- Registered a nerd patch_symbol tool that performs file IO, execution-context md5 checks, writes the patched content, and returns bounded patch metadata.
- Added focused tests for the pure AST patcher and the file-mutating tool adapter.

---

### Add AST-backed ensure import tool [07:24:49] <yiwwt8wika>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tooling

**User request summary:** Implement an AST-specific ensure_import(file_path, import_spec) tool.

**Implementation summary:**
- Added nerd-ast ensure_import_in_content to detect existing imports, insert missing import specs using AST import ranges or language-aware top-of-file fallback, and parse-check the result.
- Registered a nerd ensure_import tool that performs file IO, execution-context md5 checks, writes changed content, and reports whether the import was inserted or already present.
- Added focused tests for AST import insertion/idempotence and the file-mutating tool adapter.

---

### Add AST-backed reference search tool [07:29:25] <6ssjhxfmka>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tooling

**User request summary:** Implement the AST-backed tool find_references(symbol, scope?).

**Implementation summary:**
- Added nerd-ast find_references support that scans a file or directory scope with tree-sitter parsers and returns identifier-like AST leaf matches with ranges and source lines.
- Registered a nerd find_references tool with symbol and optional scope inputs, plus prompt schema integration.
- Added focused tests for the pure AST reference search and the Nerd tool adapter.

---

### Add AST-backed symbol test finder [07:35:58] <e7620igrka>

🟢 OpenAI Codex (`gpt-5.4`) [feat/ast-tree-sitting]

**Tags:** #nerd #ast #tooling

**User request summary:** Implement the AST-backed tool find_tests_for_symbol(file_path, symbol).

**Implementation summary:**
- Added nerd-ast find_tests_for_symbol support that scans same-language source and test files from the nearest project root, finds test-like functions or methods, and requires AST identifier references to the requested symbol.
- Registered a nerd find_tests_for_symbol tool with file_path and symbol inputs plus prompt schema integration.
- Added focused tests for unit-test and integration-test discovery and for the Nerd tool adapter.

---

### Render mention popup above input [13:13:37] <iurxk6yxra>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Render the existing @-mention popup state so file mentions are visible after typing @.

**Implementation summary:**
- Added mention popup rendering in the TUI draw path above the input box.
- Displayed visible mention candidates with selected-row highlighting and an empty-state message.

---

### Restrict mention popup trigger [13:15:58] <6419mtrzra>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Only show the @-mention popup when @ starts the input or follows whitespace.

**Implementation summary:**
- Added an input trigger predicate so embedded @ characters do not open the mention popup.
- Added event tests covering beginning-of-input, whitespace-prefixed, and embedded @ trigger behavior.

**Explicit user constraints:**
- The popup must only appear when @ is at the beginning of input or immediately follows whitespace.

---

### Adjust mention popup display [13:19:10] <ex96ne81ra>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Increase visible mention popup options from 5 to 8 and remove the popup title.

**Implementation summary:**
- Raised the mention popup visible candidate cap to 8.
- Removed the Mention title from the popup block while preserving the bordered list.

---

### Refine directory mention tabbing [13:23:52] <a6cc7st5ra>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Make Tab complete highlighted directory mentions first, then commit them only when the typed text already matches the directory path.

**Implementation summary:**
- Split mention Tab handling from Enter so directories can be completed without closing the popup.
- Added helpers for replacing only the text after the active @ trigger and refreshing the popup filter.
- Added event tests for first-Tab directory completion and second-Tab directory commit.

**Explicit user constraints:**
- A directory mention should only be fully committed by Tab when the text after @ equals the selected directory path.

---

### Append space after mention commit [13:24:38] <u3at33e6ra>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** After committing a mention, append one space after the @ path.

**Implementation summary:**
- Updated mention commit handling to append a trailing space after the committed path.
- Adjusted file, directory, and Enter commit tests to expect the trailing space.

**Explicit user constraints:**
- Directory completion before commit must not add the trailing space until the path is actually committed.

---

### Mark committed directory mentions [13:25:48] <6evir6a7ra>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Append / to committed directory mention paths.

**Implementation summary:**
- Formatted committed directory mentions with a trailing slash before the existing post-commit space.
- Updated the directory Tab commit test to expect @dir/ followed by a space.

**Explicit user constraints:**
- Only committed directory paths get the slash; first-Tab directory completion remains unslashed filter text.

---

### Add input history navigation [13:30:45] <efmfco3asa>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Use vertical arrow keys in the input box to scan through prior inputs like a POSIX terminal.

**Implementation summary:**
- Added in-memory submitted input history state with cursor and draft restoration.
- Changed Up and Down outside modals and mention popups to recall previous and next inputs, leaving PageUp and PageDown for conversation scrolling.
- Recorded submitted non-empty inputs and reset history browsing when users edit or paste input.
- Added event tests for Up and Down traversal, draft restoration, and editing recalled input.

**Explicit user constraints:**
- Mention popup Up and Down navigation remains active while the popup is open.

---

### Revert input history navigation [13:34:36] <68e57r1dsa>

🟢 OpenAI Codex (`gpt-5.4`) [feat/mentions-of-file-tree]

**Tags:** #xoxo-tui

**User request summary:** Revert the prompt-history arrow navigation because it slowed down app behavior.

**Implementation summary:**
- Removed the in-memory input history state, helper methods, and tests.
- Restored Up and Down outside popups and modals to conversation scrolling.

**Explicit user constraints:**
- Kept the mention popup changes intact while reverting only the prompt-history navigation implementation.