### Allow TUI history text selection [04:14:35] <ioe9iupul9>

🟢 OpenAI Codex (`gpt-5`) [feat/fixes-to-tui-layout]

**Tags:** #xoxo-tui

**User request summary:** Make the chat history field in the TUI selectable for copying.

**Implementation summary:**
- Stopped enabling Crossterm mouse capture when entering the alternate screen so terminal drag selection can select chat history text.
- Removed the mouse-wheel scrolling hint from the in-app help because keyboard scrolling remains the reliable navigation path without mouse capture.

**Explicit user constraints:**
- Keep the change scoped to terminal text selection behavior.

---

### Handle multiline paste in TUI input [04:19:09] <mbgv8y7xl9>

🟢 OpenAI Codex (`gpt-5`) [feat/fixes-to-tui-layout]

**Tags:** #xoxo-tui #paste-handling

**User request summary:** Make multiline pasted content stay in the TUI input box instead of triggering Enter behavior for each newline, and show a pasted-content placeholder.

**Implementation summary:**
- Enabled Crossterm bracketed paste for the TUI lifecycle so multiline paste arrives as one paste event.
- Handled paste events by appending the full payload to the input buffer without creating a pending submission.
- Rendered multiline input as a compact placeholder showing the pasted line count while preserving the full buffer for final submission.
- Added tests for multiline paste buffering, one-shot Enter submission, and placeholder line counting.

**Explicit user constraints:**
- Show a placeholder like pasted content with an X line count instead of full multiline contents.

---

### Fix q character input regression [04:41:17] <ikkdex5em9>

🟢 OpenAI Codex (`gpt-5`) [feat/fixes-to-tui-layout]

**Tags:** #xoxo-tui #input-handling

**User request summary:** Fix the TUI input field terminating the application while typing normal text.

**Implementation summary:**
- Removed the global single-key q quit path so printable q characters are inserted into the input buffer normally.
- Handled application exit through the existing `/quit` command on Enter instead.
- Added regression tests for typing text containing q and for `/quit` command exit behavior.

**Explicit user constraints:**
- Restore normal typing behavior in the bottom input field.